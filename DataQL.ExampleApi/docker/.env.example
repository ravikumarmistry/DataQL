# Owned by DataQL.ExampleApi (source key: p_sqlserver)
# cp .env.example .env && docker compose up -d
# Uses port 1434 so it can run alongside the test stack (1433).

DATAQL_SOURCE_KEY=p_sqlserver

MSSQL_HOST=localhost
MSSQL_PORT=1434
MSSQL_USER=sa
MSSQL_SA_PASSWORD=Your_strong_Password123
MSSQL_DATABASE=DataQL
